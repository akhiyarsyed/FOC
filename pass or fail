#include<stdio.h>
int main(){
    int marks;
    printf("enter marks \n");
    scanf("%d", &marks);
    if(marks <= 30 && marks >= 0)
    printf("FAIL");
    else if(marks > 30 && marks <=100)
    printf("PASS");
    else printf("INVALID MARKS");
    return 0;
}
